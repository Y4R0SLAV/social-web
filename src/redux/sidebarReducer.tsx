let initialState = {}

export type InitialStateType = typeof initialState 

const sidebarReducer = (state = initialState, action: null): InitialStateType => {
  return state
}

export default sidebarReducer